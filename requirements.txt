# gtm-research dependencies.
#
# Core (needed to run the waterfall):
requests>=2.28
PyYAML>=6.0
#
# Optional — install only if you want the feature:
#   psycopg2-binary>=2.9   -> the OPTIONAL telemetry + shared cache (RESEARCH_DATABASE_URL).
#                             Without it, the engine runs fine with no cache / no telemetry.
#   beautifulsoup4>=4.11   -> cleaner native HTML→text on the free fetch rung.
#                             Without it, the raw HTML is passed through.
#
# Install the optional extras with:
#   pip install psycopg2-binary beautifulsoup4
